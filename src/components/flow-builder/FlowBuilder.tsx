import { PlaceholderPanel } from '../shared/PlaceholderPanel';

export function FlowBuilder() {
  return (
    <PlaceholderPanel
      icon="🔀"
      title="Flow Builder"
      description="Visual drag-and-drop flow editor. Create stages, configure them, and connect dependencies."
    />
  );
}
