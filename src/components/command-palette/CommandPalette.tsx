import { PlaceholderPanel } from '../shared/PlaceholderPanel';

export function CommandPalette() {
  return (
    <PlaceholderPanel
      icon="⌨️"
      title="Command Palette"
      description="Browse and execute skills and flows. Auto-generated forms for parameters. Search by name or description."
    />
  );
}
