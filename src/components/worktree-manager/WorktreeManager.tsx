import { PlaceholderPanel } from '../shared/PlaceholderPanel';

export function WorktreeManager() {
  return (
    <PlaceholderPanel
      icon="🌳"
      title="Worktree Manager"
      description="Create, destroy, and manage git worktrees. Bulk cleanup. Each worktree is an independent workspace."
    />
  );
}
