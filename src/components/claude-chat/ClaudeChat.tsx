import { PlaceholderPanel } from '../shared/PlaceholderPanel';

export function ClaudeChat() {
  return (
    <PlaceholderPanel
      icon="🤖"
      title="Claude Chat"
      description="Interactive Claude Code session. Conversation mode and verbose mode. Embedded terminal or chat UI."
    />
  );
}
